import { env } from "@/lib/env";

/**
 * Donation link placeholder. Swap NEXT_PUBLIC_DONATION_URL in your .env for
 * a real Buy Me a Coffee / Ko-fi / GitHub Sponsors link before launch — see
 * docs/00-product-plan.md, "Monetization / sustainability".
 */
export const DONATION_URL = env.NEXT_PUBLIC_DONATION_URL;
